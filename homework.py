import Math
#Question 1
variables = []
quadratic_Equation = (1,2,-3)
variables.append(quadratic_Equation.split(,))
firstPart = variables[0] * 
pass

#Question 2
def fraction(self):
    sumOfFractions([[18, 13], [4, 5]]) ➞ 2
    fractionPartList = []
    fractionPartList = sumOfFractions.split(",")
    a = fractionPartList[0][0]
    b = fractionPartList[0][1]
    c = fractionPartList[1][0]
    d = fractionPartList[1][1]
    if b != d:
        e = b * d
        firstFraction = a / e
        secondFraction = c / e
        finalNumerator = a + c
        finalAnswer = finalNumerator / e
    else: 
        firstFraction = a / b
        secondFraction = c / d
        finalNumerator = a + c
        finalAnswer = finalNumerator / b

#Question 3
def findLongestWord(self):
    sentence = "Margaret's toy is a pretty doll."
    longestWord = []
    longestWord = sentence.split(" ")
    for word in len(longestWord):
        currentLongestWord = 0
        if len(word) > lengthOfLongestWord:
            lengthOfLongestWord = len(word)
            longestWord = word
    print(longestWord)
